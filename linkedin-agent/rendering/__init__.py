"""HTML rendering package.

Named `rendering` (not `html`) to avoid shadowing Python's stdlib `html` module.
Turns RunContext data into self-contained review dashboards via Jinja2 templates
in templates/html/.
"""
