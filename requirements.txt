# Purands AI Platform — Python dependencies
# Pinned to minor versions for reproducible CI runs.

anthropic>=0.39,<1.0        # LLM provider (Claude)
openai>=1.40,<2.0          # LLM provider (OpenAI / GPT)
PyYAML>=6.0,<7.0            # config + sources YAML
Jinja2>=3.1,<4.0           # HTML report rendering
feedparser>=6.0,<7.0       # RSS / Atom feed parsing
requests>=2.31,<3.0        # HTTP for blog/news/url/github sources
beautifulsoup4>=4.12,<5.0  # HTML parsing for blog/news sources
python-dateutil>=2.8,<3.0  # robust date handling

# --- dev / test ---
pytest>=8.0,<9.0
Flask>=3.0,<4.0            # Comment Studio (interactive local tool)
